pipeline {
  agent any
  stages {
    stage('Init') {
      steps {
        echo 'init'
      }
    }
    stage('Build') {
      steps {
        echo 'Build'
      }
    }
    stage('Deploy') {
      steps {
        echo 'Deploy'
      }
    }
  }
}
